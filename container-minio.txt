creation du container :

docker run -p 9000:9000 --name minio1 \
  -e "MINIO_ACCESS_KEY=testaccesskey" \
  -e "MINIO_SECRET_KEY=testsecretkey" \
  -v /mnt/data:/data \
  -v /mnt/config:/root/.minio \
  minio/minio server /data
